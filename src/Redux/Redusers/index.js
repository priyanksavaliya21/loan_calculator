import { combineReducers } from "redux";
import { TotalResultstRedusers } from "./Redusers1";

const RootRedusers = combineReducers({
  layer: TotalResultstRedusers,
});

export default RootRedusers;
